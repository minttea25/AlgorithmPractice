#include <iostream>
#include <vector>
#include <algorithm>

#define _x(pos) pos.first
#define _y(pos) pos.second

using namespace std;

using pos = pair<int, int>;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    

    return 0;
}